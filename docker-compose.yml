version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: blog-postgres-local
    restart: unless-stopped
    environment:
      POSTGRES_DB: blog_mern
      POSTGRES_USER: blog_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-BlogLocalPass2024!}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - blog-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U blog_user -d blog_mern"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend-mern
      dockerfile: Dockerfile
    container_name: blog-mern-backend-local
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://blog_user:${DB_PASSWORD:-BlogLocalPass2024!}@postgres:5432/blog_mern?schema=public
      JWT_SECRET: ${JWT_SECRET:-local_dev_jwt_secret_change_this_minimum_32_chars}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-local_dev_refresh_secret_change_this_minimum_32_chars}
      JWT_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 7d
      FRONTEND_URL: http://localhost:8080
      CORS_ORIGINS: http://localhost:8080,http://localhost:3001
    ports:
      - "3000:3000"
    volumes:
      - backend_uploads:/app/uploads
    networks:
      - blog-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        VITE_BACKEND_TYPE: mern
        VITE_API_URL: http://backend:3000/api
        VITE_BACKEND_URL_SERVER: http://backend:3000
        VITE_SITE_URL: http://localhost:8080
        VITE_SITE_NAME: "Modern Blog Leader"
        VITE_LOCALES: fr,en
        VITE_DEFAULT_LOCALE: fr
        NODE_ENV: production
    container_name: blog-mern-frontend-local
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - blog-network
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  blog-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  backend_uploads:
    driver: local
